<div class="container">
  <% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant)  %></p>
  <%= link_to "🗑", restaurant, method: :delete, remote: true %>
  <% end %>
</div>
